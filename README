----------------
---2017/11/04---
----------------
1. やることリスト
N64 BIdisperse 1:3 and 3:1の統計が取れてきたのでそれをまとめる。
鞍点落としなし(エネルギー評価のみ)でのジャミング転移点見積もりについてexponentが不変かどうか調べる。
固有値方程式をスケーリングした際に、なぜ総質量を同じにするようにリスケールすると状態密度の形が同じになっていたのか詳しく調べる。
=> 合っているように見えただけなのではないか。
=>\tilde{\omega} = C^{-1/2}\omegaではなく、 (MASSのリスケーリング)
=>\tilde{\omega} = C^{-2/d}\omegaなのではないか。(粒子サイズを考慮した、Hessianのリスケーリング)



============================
========戯言メモ一覧========
============================
統計取る方でジャミング転移点を見積もる際に、末尾でエネルギーの評価を行う.
エラー書き込みを複数iterで行えるようにする.
９．２６やることリスト
ok! シミュレーションで緩和時間を記述するように修正。
ok! 粒子数ごとに適切な緩和時間を決めておく。
鞍点サンプリングと普通のやつでの違いexponent的にも異なるのかみたいなところを考える. (統計とるので時間かかる.)
N増やしたいね。
局所的にさいみつ充填クラスターの欠陥が入った結晶格子模型？
質量のスケーリング。（１：１BIを基準にしておくか。。。粒子密度は一定になっているが、混合比の異なる系との比較において系全体の質量が保たれていない。）
  <<シミュレーション上でスケールしちゃうか否か。数式上どうなるかはわかったので、グラフ上でスケールしてもいい。
  MONO 1:0を基準にした時(総mass=N) Factor=N/(Tr[M]), scaled omega = omega/sqrt(Factor)として、ただ単に掛け算してやればよい。
せん断スペクトル or フォノンオーダーパラメータ（どういったパラメータか論文を読んで理解する）
低周波はフォノン(extended & phonon)とローカライズモード(localized & non-phonon)のカップリング(ごちゃまぜ)ではなく、本当に線形結合的にかけるのか。
  その固有振動数におけるIPRの分布を見てやる。(2極化するか否か.)
  << IPRの分布をBI1:1で見てみたところ、論文のように二極化する様子は見られなかった... (phononモード(Debye的)と言われている協調的な分枝が見られなかった。)
ポテンシャルローカルミニマムの探索と、マスを持った粒子の実際の運動（固有振動）
ジャミング転移点が低い・高いというのはモードで特徴づけられる話ではないのか。ゆるいモード硬モード
混合比について、結晶化近くのMONOに限りなく近い比を取ったらどうなるか?
ok! Qt3DのプログラムをQt5.9verにmodify.
シミュレーションプログラムの引数が多くなってきたのでクラス実装したほうがよいのではないか。

N64でエネルギーゼロになる不具合...
  <<おそらくcontactListのときのOutOfPeriodicBoundary関数で評価が上手くできていない可能性.
  ok! <<いや、E=0(unjammed state)になった瞬間即breakにしているが、そうすると不正なcontactListでゼロになるパターンを排除できていない...! (要修正.)
    << もはやN<=128のときはcontactList使わない説.
    << 系のサイズLの半分よりも大きかったら半径よりちょっと大きいくらいにセットし直す
    << (とりあえずでかい粒子の半径を基準にしているが,自身の半径のほにゃらら倍とかにしておけば良いのでは?)

modifyしたプログラム(N依存でMD反復回数を変更)にバグ(N2048が異常な動き。N256,512はうまくいってるっぽい。)
 << 上限MDステップがオーバーフローして負になってた。修正済み。
計算時間ヤヴァイ. 
 << 固有値計算に圧倒的時間を費やしているので、固有値計算の数をそもそも減らす。
 << 鞍点のサンプル => 緩和時間が長い というロジックが成り立つならば固有値計算せずにノイズかけてもっかい緩和かけてみるとか?

緩和だけのmethodだと、どこで緩和したとみなすかを決めるのが微妙。
-> determine eng break threshold larger than machine epsilon.
-> のいずを加えることも検討する。

今後の方針。
混合比異なるsampleについても計算してまとめる.
-> omegaに(\sum_{i=1}^{N}m_1)/(\sum_{i=1}^{N_1}m_1 + \sum_{i=N_1+1}^{N} m_2) の逆数をかけて、
->スケーリングの混合比依存性を次回発表する。結合の仕方に依らずN->infinityで平均場と同じ振る舞いになることを示す.
->実装としては、追加情報としてomegaのデータに混合比とサイズ比を入れる。演算でMONOとBIの場合分け.
=>omega_rescaledを生成.

EMTの理論で混合比に依らず成り立つことを解析的に証明してみる.(できるかはわかんないけどパラメータ一個増えたり取扱が面倒になるだけなので問題なさそう.)
(済) phonon order parameterについて紹介して、もーどの性質をまとめる.
実際に自身の研究でParticipation Ratioをプロットしてみてどうなってるか見てみる。

直近のゼミまで
(済)N=64のシミュレーション計算についてバグ修正 （急いで取り掛かる）
論文まとめ、参考資料箇所ちゃんと調べ直す。随時パワポに書き起こす
N=1024のデータについて統計取り直す。もう一度異混合比系でのグラフ確認。
一番小さい固有値を求めるメソッドを追加する。
ジャミング転移点みつもり、固有値計算なしで大丈夫か。
マスのスケーリングをしたら、どうして状態密度の結果が一致したのか。理論的検証。数式書いて色々考える。
N64足してみよう。
N1024もっとサンプル増やそう。
MONOの結果を足しておこう。
Participation Ratio について自分の系についても出しておこう。論文と比較。


